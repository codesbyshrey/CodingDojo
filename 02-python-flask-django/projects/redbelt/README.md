# Redbelt

Red belt review project variant preserved because it differs from the main Python course copy.

Block: `Python, Flask, and Django Block`

## Revisit Notes

- Flask-style `server.py` entrypoint is present.

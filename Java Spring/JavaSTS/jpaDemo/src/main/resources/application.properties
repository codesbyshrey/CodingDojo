# Necessary to pass data from controller to .jsp file
spring.mvc.view.prefix=/WEB-INF/

# code to configure application to connect to data source
spring.datasource.url=jdbc:mysql://localhost:3306/book-schema
spring.datasource.username=root
spring.datasource.password=rootroot
spring.jpa.hibernate.ddl-auto=update
# other properties here (removed for brevity)
spring.mvc.hiddenmethod.filter.enabled=true

